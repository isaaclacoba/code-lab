# Work log

- Start: 2026-06-19 18:11:07 | Task: Turn the compiler host into a generic, language-agnostic compile/run/milestone engine. The exercise (content + structural checks) must be owned by the consumer (a course/level) and injected at build time, not baked into code-lab. code-lab today runs C# but must extend to Python, Kotlin, C++, etc.
- End: 2026-06-19 18:11:07 | Result: Introduced the IExercise contract (Services/Exercise.cs: Hint, CodeAnchor, Milestone, MilestoneResult, IExercise) under namespace CodeLab.Host. ExerciseLoader.Load() discovers an IExercise implementation by reflection and falls back to a built-in SampleExercise when none is supplied. CompilerService is now generic (no capstone references). Home.razor renders Title/Lead/Brief/Milestones and calls Exercise.Check; all JS interop renamed level3Capstone.* -> codeLabHost.*. Program.cs registers IExercise via ExerciseLoader; _Imports/RunnerBridge updated. New CodeLabHost.csproj (RootNamespace CodeLab.Host, AssemblyName CodeLabHost) with a conditional Compile include driven by the ExerciseSource MSBuild property, so a consumer supplies their exercise as a folder of .cs files. Removed the capstone-specific Capstone.cs, StructuralChecks.cs and Level3Capstone.csproj (moved to the course). Verified: standalone build (SampleExercise) compiles; course-injected build compiles and grades the capstone correctly end-to-end.
